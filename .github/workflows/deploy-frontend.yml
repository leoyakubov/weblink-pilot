name: Deploy frontend

on:
  workflow_dispatch:
  workflow_run:
    workflows: [CI]
    types: [completed]

jobs:
  deploy:
    if: github.event_name == 'workflow_dispatch' || (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main')
    runs-on: ubuntu-latest
    environment: demo
    env:
      VITE_API_BASE_URL: ${{ secrets.VITE_API_BASE_URL }}
      NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
      NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
          fetch-depth: 2

      - name: Detect frontend changes
        id: changes
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "frontend_changed=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          changed_files="$(git diff --name-only HEAD^ HEAD)"
          echo "Changed files:"
          printf '%s\n' "$changed_files"

          if printf '%s\n' "$changed_files" | grep -E '^(frontend/|\.github/workflows/deploy-frontend\.yml$)' >/dev/null; then
            echo "frontend_changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "frontend_changed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Skip deploy when frontend did not change
        if: steps.changes.outputs.frontend_changed != 'true'
        run: |
          echo "No frontend-relevant files changed; skipping frontend deploy."
          exit 0

      - name: Set up Node 22
        if: steps.changes.outputs.frontend_changed == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        if: steps.changes.outputs.frontend_changed == 'true'
        working-directory: frontend
        run: npm ci

      - name: Run frontend tests
        if: steps.changes.outputs.frontend_changed == 'true'
        working-directory: frontend
        run: npm test -- --run

      - name: Build frontend
        if: steps.changes.outputs.frontend_changed == 'true'
        working-directory: frontend
        run: npm run build

      - name: Deploy to Netlify
        if: steps.changes.outputs.frontend_changed == 'true'
        working-directory: frontend
        run: npx --yes netlify-cli deploy --dir=dist --prod --site="$NETLIFY_SITE_ID" --auth="$NETLIFY_AUTH_TOKEN"
