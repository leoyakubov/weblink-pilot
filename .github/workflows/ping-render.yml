name: Ping Render

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    environment: demo

    steps:
      - name: Ping backend health endpoint
        env:
          RENDER_HEALTH_URL: ${{ vars.RENDER_HEALTH_URL || secrets.RENDER_HEALTH_URL }}
        run: |
          set -euo pipefail

          if [ -z "${RENDER_HEALTH_URL:-}" ]; then
            echo "RENDER_HEALTH_URL is not set in the demo environment."
            exit 1
          fi

          curl --fail --silent --show-error --max-time 30 "$RENDER_HEALTH_URL" >/dev/null
          echo "Pinged $RENDER_HEALTH_URL"
