title = "WeblinkPilot Gitleaks configuration"

[extend]
useDefault = true

[[allowlists]]
description = "Ignore generated build output directories"
paths = [
    '''(^|/)(target|dist|coverage|node_modules)(/|$)''',
    '''(^|/)\.git(/|$)''',
]
